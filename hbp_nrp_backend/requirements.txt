hbp-nrp-commons
# Web stack (EBR2-65). Off the Flask 2 / Werkzeug 2 line and the retired
# Flask-RESTful: the REST layer now uses Flask-Smorest (flask.views.MethodView
# blueprints + marshmallow schemas). Python 3.11 (EBR2-64) stays gated on the
# nrp-core base image shipping 3.11, so it is out of scope here.
Flask>=3,<4
Werkzeug>=3,<4
flask-smorest>=0.44,<1
marshmallow>=3.19,<5
# urllib3 2.x; requests>=2.32 already supports it on the Python 3.10 base.
urllib3>=2,<3
requests>=2.32,<3
cryptography>=42.0
# Bounded TTL cache for auth-token / experiment-access memoization
# (replaces the unbounded functools.lru_cache that cached failures forever).
cachetools>=5.0,<8
# pytz (Europe/Zurich clone timestamps in hbp_nrp_backend + hbp_nrp_simserver)
# was previously pulled in transitively by the now-removed Flask-RESTful; pin it
# explicitly so the runtime venv still ships it.
pytz>=2022.1
