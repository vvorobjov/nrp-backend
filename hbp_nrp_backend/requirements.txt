hbp-nrp-commons
# Web stack — bumped under EBR2-46 first slice (Python 3.8 still).
# Off the Werkzeug<=2.1.2 ceiling that has blocked further upgrades
# since 2022. Flask 2.3+ is the maximum compatible with the still-
# pinned Python 3.8 base image; Flask 3 + the Flask-RESTful retire
# move to Flask-Smorest land in EBR2-46b.
Flask>=2.3,<3
Werkzeug>=2.3,<3
Flask-RESTful>=0.3.10
# urllib3 dropped the [secure] extras-name in 2.x; keep on the 1.26
# series so we can stay on Python 3.8 + the existing requests pin.
urllib3>=1.26,<2
requests>=2.32,<3
cryptography>=42.0
